# DeepChess
The objective of this project is to design and implement a deep learning-based chess engine that replaces traditional hand-crafted heuristic evaluations with a neural network.  The core of the system will be a position evaluation model, for which we intend to explore either a Convolutional Neural Network (CNN) or a Vision Transformer (ViT).
